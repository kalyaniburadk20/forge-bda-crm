# The client uses Vite's dev proxy (see vite.config.js) to reach the API at
# http://localhost:5000, so no env vars are required for local development.
# For production, point the proxy or axios baseURL at your deployed API URL.
